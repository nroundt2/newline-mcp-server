jenkins:
  releaseNotes:
    autoGenerate: true
build:
  commands:
    # Build the docker container
    - type: "zip"
      options:
        name: "customrelease"
        useExactZipName: false
        directory: "hello"
        excludes: "BuildInfo.txt"
        archive: true
